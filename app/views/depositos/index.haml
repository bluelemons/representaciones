.ui-widget
  %h1.ui-widget-header Depositos
  
  .action_bar
    -if can? :new, Deposito
      = link_to 'Nuevo Deposito', new_deposito_path,:remote => true 
    = link_to 'Imprimir', {:search=>params[:search],:format=>'pdf'} 
  #deposito_form.half.right
  
  = render :partial => "search"
  #depositos
    =render 'depositos'
