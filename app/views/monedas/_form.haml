= form_for(@moneda,:remote=>true) do |f| 
  .ui-widget.ui-widget-content
    - if @moneda.errors.any? 
      #error_explanation
        %h2="#{pluralize(@moneda.errors.count, 'error')} prohibited this moneda from being saved:"
  
        %ul
          - @moneda.errors.full_messages.each do |msg| 
            %li= msg 
    
    
    .field
      = f.label :Name
      %br
      = f.text_field :name
    
    
    .actions
      = f.submit 
