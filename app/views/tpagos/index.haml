.ui-widget
  %h1.ui-widget-header Tpagos
  
  .action_bar
    -if can? :new, Tpago
      = link_to 'Nuevo Tpago', new_tpago_path,:remote => true 
    = link_to 'Imprimir', {:search=>params[:search],:format=>'pdf'} 
  #tpago_form.half.right
  
  = render :partial => "search"
  #tpagos
    =render 'tpagos'
