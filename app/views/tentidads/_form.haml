= form_for(@tentidad,:remote=>true) do |f| 
  .ui-widget.ui-widget-content
    - if @tentidad.errors.any? 
      #error_explanation
        %h2="#{pluralize(@tentidad.errors.count, 'error')} prohibited this tentidad from being saved:"
  
        %ul
          - @tentidad.errors.full_messages.each do |msg| 
            %li= msg 
    
    
    .field
      = f.label :Name
      %br
      = f.text_field :name
    
    
    .actions
      = f.submit 
