<h1>Nuevo Pase</h1>
<%= form_for @pase_form.pase do |f| %>

  <p>
  <%= f.label :origen_id %>
  <%= f.select :origen_id, @pase_form.reservas_con_saldo %>
  </p>

  <p>
  <%= f.label :fecha %>
  <%= f.date_select :fecha, default: Date.today, end_year: Date.today.year %>
  </p>

  <p>
  <%= f.label :monto %>
  <%= f.text_field :monto %>
    <span>USD<span>
  </p>

  <p>
  <fieldset>
    <legend>Cuentas afectadas</legend>
    <label>
      <%= f.radio_button("cuentas_afectadas", "agencia") %>
      Agencia
    </label>
    <label>
      <%= f.radio_button("cuentas_afectadas", "operadora") %>
      Operadora
    </label>
    <label>
      <%= f.radio_button("cuentas_afectadas", "ambas", checked: true) %>
      Ambas
    </label>
  </fieldset>
  </p>

  <div class="destinos">
    <p>
    <%= f.label :destino_id %>
    <%= f.select :destino_id, @pase_form.posibles_reservas, prompt: "Indique destino del pase" %>
    </p>

    <p>
    <%= f.label :monto %>
    <%= f.text_field :monto %>
    <span>USD<span>
    </p>

    <p>
    <%= f.label :destino_id %>
    <%= f.select :destino_id, @pase_form.posibles_reservas, prompt: "Indique destino del pase" %>
    </p>

    <p>
    <%= f.label :monto %>
    <%= f.text_field :monto %>
    <span>USD<span>
    </p>

    <p>
    <%= f.label :destino_id %>
    <%= f.select :destino_id, @pase_form.posibles_reservas, prompt: "Indique destino del pase" %>
    </p>

    <!--<p><%# @pase_form.posibles_reservas %></p> -->

    <p>
    <%= f.label :monto %>
    <%= f.text_field :monto %>
    <span>USD<span>
    </p>
  </div>

  <p>
  <%= f.submit %>
  </p>
<% end %>

<script>
</script>

<style>
h1 {
  margin: 20px;
}

form p label {
  display: inline-block;
  width: 90px;
  text-align: right;
}

fieldset {
  width: 400px;
  margin: 20px;
}

label input {
  margin: 3px;
}

form p {
  margin-bottom: 5px;
}

.destinos {
  padding-left: 115px;
}
</style>
