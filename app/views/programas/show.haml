%p#notice= notice 

.ui-widget
  .action_bar
    -if can? :new, Programa
      = link_to 'Nuevo', new_programa_path,:remote =>true
    -if can? :edit, @programa 
      = link_to 'Editar', edit_programa_path(@programa),:remote =>true
    = link_to 'Todos', programas_path ,:remote =>true
=render 'programa'
