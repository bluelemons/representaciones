= form_for(@pasajero,:remote=>true) do |f| 
  .ui-widget.ui-widget-content
    - if @pasajero.errors.any? 
      #error_explanation
        %h2="#{pluralize(@pasajero.errors.count, 'error')} prohibited this pasajero from being saved:"
  
        %ul
          - @pasajero.errors.full_messages.each do |msg| 
            %li= msg 
    
    
    .field
      = f.label :Doc
      %br
      = f.text_field :doc
    
    .field
      = f.label :Name
      %br
      = f.text_field :name
    
    .field
      = f.label :Nacimiento
      %br
      = f.text_field :nacimiento
    
    .field
      = f.label :Tdoc_id
      %br
      = f.text_field :tdoc_id
    
    
    .actions
      = f.submit 
