.ui-widget
  %h1.ui-widget-header Tarifa
  
  .action_bar
    -if can? :new, Tarifa
      = link_to 'Nuevo', tarifas_path,:remote => true 
  #tarifa.half.right
  
    = render :partial => "edit"
  
  #tarifas.half.left
