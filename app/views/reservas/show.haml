%p#notice= notice 

.ui-widget
  .action_bar
    -if can? :new, Reserva
      = link_to 'Nuevo', new_reserva_path,:remote =>true
    -if can? :edit, @reserva 
      = link_to 'Editar', edit_reserva_path(@reserva),:remote =>true
    = link_to 'Todos', reservas_path ,:remote =>true
=render 'reserva'
