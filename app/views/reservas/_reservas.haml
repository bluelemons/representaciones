=will_paginate @reservas
#reservas_grid
  =grilla(["ID", "Titular", "Referencia", "Salida", "Deuda Agencias", "Deuda Operadora", "Mon", "Acciones"], @reservas, [:id, :titular, :referencia, :salida, :agencia_deuda_format, :operadora_deuda_format, :total_currency], {:id=>"reserva_grid", :class=>"togrid"})
.action_bar
  =link_to "Imprimir", {:search=>params[:search], :format=>'pdf'}
  =link_to "Exportar", {:search=>params[:search], :format=>'csv'}

