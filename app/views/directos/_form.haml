= form_for(@deposito,:remote=>true) do |f|
  =flash[:notice]
  .ui-widget.ui-widget-content
    = error_message(@deposito)

    = render :partial => "movimientos/comunes",:locals=>{:f=>f}
    Agencia
    %br
    = f.collection_select(:entidad_id, Agencia.all, :id, :name, :prompt => false)
    %br
    Operadora
    %br
    = f.collection_select(:entidad_id, Operadora.all, :id, :name, :prompt => false)

    #reservas_search.qs_search
    #reservas{ :style => 'height:250px' }
    %table
      %tr
        =render :partial =>'montos/form',:locals=>{:f=>f}


    = f.label :Tipo_de_deposito
    %br
    = collection_select(:deposito, :tdeposito_id, Tdeposito.all, :id, :name, :prompt => false)

    = f.text_field :numero,:size=>10

    .actions
      = f.submit

