= form_for(@tdoc,:remote=>true) do |f| 
  .ui-widget.ui-widget-content
    - if @tdoc.errors.any? 
      #error_explanation
        %h2="#{pluralize(@tdoc.errors.count, 'error')} prohibited this tdoc from being saved:"
  
        %ul
          - @tdoc.errors.full_messages.each do |msg| 
            %li= msg 
    
    
    .field
      = f.label :Name
      %br
      = f.text_field :name
    
    
    .actions
      = f.submit 
