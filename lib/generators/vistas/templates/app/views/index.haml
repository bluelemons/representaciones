.ui-widget
  %h1.ui-widget-header <%= plural_name.capitalize %>
  
  .action_bar
    = link_to 'Nuevo <%= file_name_c %>', new_<%= file_name %>_path,:remote => true 
    = link_to 'Imprimir', {:search=>params[:search],:format=>'pdf'} 
  #<%= file_name %>.half.right
  
  = render :partial => "search"
  
  .half.left
    %h3.ui-widget-header Listado
    #<%= plural_name %>
      =render '<%= plural_name %>'
